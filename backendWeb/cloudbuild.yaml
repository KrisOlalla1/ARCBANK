steps:
# Paso 1: Construir la imagen Docker
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-t', 'gcr.io/$PROJECT_ID/bancaweb-service', '.']

# Paso 2: Empujar la imagen al registro
- name: 'gcr.io/cloud-builders/docker'
  args: ['push', 'gcr.io/$PROJECT_ID/bancaweb-service']

# Paso 3: Desplegar en Cloud Run
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  entrypoint: gcloud
  args:
    - 'run'
    - 'deploy'
    - 'bancaweb-service'
    - '--image'
    - 'gcr.io/$PROJECT_ID/bancaweb-service'
    - '--region'
    - 'us-central1'
    - '--port'
    - '8080'
    - '--no-allow-unauthenticated'
    - '--add-cloudsql-instances'
    - 'aqueous-depth-478400-k3:us-central1:arcbank-db-instance'
